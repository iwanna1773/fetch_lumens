import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'fetch_lumens',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
